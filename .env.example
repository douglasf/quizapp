# Cloudflare Worker URL for image uploads
# Set this to your deployed Worker URL (see worker/README.md for setup instructions)
# Leave empty or remove to disable cloud upload (images will be embedded inline as base64)
#
# Examples:
#   VITE_IMAGE_WORKER_URL=https://quiz-image-worker.your-account.workers.dev
#   VITE_IMAGE_WORKER_URL=http://localhost:8787  (for local Worker development)
VITE_IMAGE_WORKER_URL=

# QuizApp API URL for authentication and quiz CRUD
# Set this to your deployed API Worker URL
# Defaults to http://localhost:8787 if not set
#
# Examples:
#   VITE_API_URL=https://quizapp-api.your-account.workers.dev
#   VITE_API_URL=http://localhost:8787  (for local development)
VITE_API_URL=
